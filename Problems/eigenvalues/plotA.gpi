set term svg size 640,480 background "white" font "times,14"
	set out "plotA2.svg"
	set key left
	set title "Quantum particle in a box: eigenfunctions"
	set xlabel "x"
	set ylabel "Psi(x)"
	plot "outA2.data" with lines\
	,sqrt(2) with lines\
	,-sqrt(2) with lines
